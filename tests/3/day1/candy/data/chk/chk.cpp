#include "testlib.h"

int main(int argc, char* argv[])
{
    // registerTestlibCmd(argc, argv);

    // while (!ouf.seekEof()) {
    //     ouf.readString();
    // }
    // quitf(_unexpected_eof, "PC");
    // quitp(90, "PC");
}